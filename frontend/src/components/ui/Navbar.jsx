import { AppBar, Button, Toolbar, Typography } from '@mui/material';

export const Navbar = () => {
  return (
    <AppBar 
    >
        <Toolbar>
            <Typography variant="h4">
                BarkBnb
            </Typography>
        </Toolbar>
    </AppBar>
  )
}
